@using University.Models
@model System.Collections.Generic.List<Student>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Enrollment Date</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    @if (Model.Count == 0)
    {
      <tr><td colspan="5">No Students Yet</td></tr>
    }

    @foreach (Student item in Model)
    {
      <tr>
        <td>@item.Name</td>
        <td>@item.EnrollmentDate</td>
        <td>
          <div class="d-flex justify-content-end">
            <a
              asp-action="Details"
              asp-route-studentId="@item.Id"
              class="btn btn-secondary mx-1"
            >See Details</a>
            <a
              asp-action="Remove"
              asp-route-studentId="@item.Id"
              class="btn btn-danger mx-1"
            >Delete</a>
          </div>
        </td>
      </tr>
    }
  </tbody>
</table>